global:
  scrape_interval: 15s

# Le scraping est désormais assuré par Grafana Alloy, qui pousse les métriques
# vers Prometheus via remote_write (--web.enable-remote-write-receiver).
# On laisse les anciens scrape_configs commentés pour référence / rollback.
#
# scrape_configs:
#   - job_name: "prometheus"
#     static_configs:
#       - targets: ["localhost:9090"]
#
#   - job_name: "tns_app"
#     # Override global settings
#     scrape_interval: 5s
#     static_configs:
#       - targets: ["app:80"]
